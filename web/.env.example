# QueueLess Web API Configuration
# Base URL for the QueueLess Backend REST API (e.g. http://localhost:8080 for local dev or https://api.queueless.com for production)
# Leave empty if using reverse-proxy/same-origin /api routing.
VITE_API_URL=
